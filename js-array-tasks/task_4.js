/*### 4. Checking if it's an Array

**Instructions:**

1. Create different variables, each containing either an array or a non-array value.

2. Now use isArray to check if each variable is an array.

3. Print a message to the console indicating whether each variable is an array or not.*/
let num =10;
let numArray=[12,12,3,4,5,6];
let nam = 'Name is name ';
Array.isArray(num)?console.log('Array'):console.log('Not Array');
Array.isArray(nam)?console.log('Array'):console.log('Not Array');
Array.isArray(numArray)?console.log('Array'):console.log('Not Array');


